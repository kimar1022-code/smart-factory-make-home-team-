# CLAUDE.md

이 저장소는 하모니(2조) 로봇셀 프로젝트의 **상황 공유·인수인계 문서 저장소**입니다.

## 작업 시작 전 반드시 읽을 것

1. `docs/` 안에서 **가장 최근 날짜의 프로젝트 메모리 문서**를 먼저 읽는다.
   (현재: **`docs/인수인계_0909_다음세션_A타입착수.md`** → `docs/RUNBOOK_0908밤_WB진범_출하리프트.md` → `docs/체크리스트_A타입착수_0909.md`)
2. 그 문서의 **"7. 반드시 지킬 규칙·함정"** 절은 실제 사고에 기반한 철칙이다.
   특히 다음은 임의로 어기지 않는다.
   - 골든·벽·티칭 값은 사용자 승인 없이 변경 금지.
   - 실패 후 부품을 랙에 되돌리는 것은 사용자가 직접(로봇 자동복귀 금지).
   - 실기 스택 가동 중 같은 DDS 도메인에서 테스트/골 전송 금지.
   - 기둥 꼭대기 아래에서 XY 횡보정 금지.
   - J6 |ΔJ6| > 180° 이동 금지.
   - 게이트(축척·rms·정렬 이동 4mm·파지 편차)는 완화하지 않는다 — 검출을 보강하거나 재파지.
   - 내벽(red_in)/외벽 코드 경로 분리. Z 값은 사용자가 정한다.
   - 사이클(:8776)이 busy 면 로봇 명령·카메라 노출·서버 재기동을 하지 않는다(409·사다리 충돌 전력).
   - 픽셀→로봇 보정은 Δ≠0 으로 부호를 시험한 뒤에만 "검증됨"(9/9 포크 부호 사고).

## 저장소 규칙

- 로봇 제어 코드는 `tools/`·`bridge_server.py`·`cam_server.py` 로 미러돼 있다(원본은 로봇 PC `~/bf2_console/`, 문서의 경로는 그 기준). 실기 반영은 로봇 PC 에서 한다.
- `cad/`의 STL이 HOUSE 치수의 1차 근거다. 치수를 인용할 때는 `tools/measure_house_stl.py`로 재현한 값을 쓴다(추정치와 구분해 표기).
- 새 메모리 문서를 추가할 때는 `docs/YYYY-MM-DD_<제목>.md` 형식을 따른다.
- 기존 문서는 덮어쓰지 말고 새 날짜 문서로 추가한다(이력 보존).
