# Smart Factory — 조립식 주택 자동 조립 로봇셀 (2조 하모니)

FR5 협동로봇 + ZeKeep ×2 + 터틀봇으로 **사람 개입 0** 조립식 주택 조립 데모.
이 저장소 = FR5 벽 파지·삽입 로봇셀 (비전 검출 + 단계형 서보잉 + 하모니 콘솔).

> 담당: 김애리(로봇SW). 마감 9/7(월). 이 README는 2026-09-05 밤 기준.

---

## 지금까지 한 것 ✅

### 조립(벽 삽입)
- **파랑·노랑·빨강(긴벽) 3벽 무보정 삽입 성공**(9/3 반복, Δ≤0.5mm) — 마운트 교체 전 기준.
- **앞끝우선 place 러너**(`tools/place_front_first.py`): 손목캠(앞끝) XY → 새카메라(뒤끝) yaw, 단계형(z478→443→413→388).
- **홈포즈 골든**: 로봇이 집은 채 홈포즈로 XYZ 조그 안착 후 골든 캡처 → 파지 편차 0.05mm → 무보정 삽입.
- **자코비안 실측** 정렬(축 매핑 발산 방지) + **최소 실행 이동량** 대응(자세마다 다른 로봇 분해능).
- **앵커 2종**: ArUco 마커(깨끗하나 책상 고정 → 베이스 이동 못 봄) / 기둥 색점 dots(베이스 이동 추종, 유령점 취약).

### 인프라 (9/4 추가)
- **하모니 콘솔**(`bf2_robot_console.html`): 트윈·조그·리코더·카메라 패널. 측면캠 패널 + 벽꽂기 플레이어(리코더 내).
- **벽 꽂기 플레이어**(`tools/wall_player.py` :8770): 세션 없이 브라우저 색버튼으로 삽입 재생.
- **카메라 자동복구 데몬**(`tools/cam_autostart.sh`): D435·새카메라 USB 재연결/얼음 자동 재기동.
- **자세별 회전 뎁스 뷰어**(`tools/depth_view_rot.py` :8772), **측면캠 스트림**(`tools/side_cam.py` :8771).
- **J6 대회전 봉인 가드**(`bridge_server.py`): |ΔJ6|>180° 이동 거부(controller_dead 동결 방지).
- **형상 검출**(`tools/edge_place.py`·`base_depth_corner.py`) + **측면 삽입감시 스캐폴드**(`tools/side_slot.py`).

---

## 지금 문제 → 재설계 방향

1. **red_s(짧은 빨강)**: 작고 느슨해 파지 3mm 변동 + 기둥 진입 후 **양 카메라 벽점 소실** → 위 카메라론 불가.
2. **베이스 이동**: ZK 흡착이 밑판을 돌리면 기둥(끼우는 자리)도 이동 → 책상 마커론 못 따라감.
3. **9/4 카메라 마운트 교체** → 골든 전량 무효.

**재설계**(`docs/재설계_베이스이동_벽삽입_0904.md`): 기준계를 **밑판 외곽선+기둥(형상, 매 사이클 재검출)**으로 → 베이스 이동 자동 흡수. **측면 카메라**로 삽입 감시(기둥 진입 후 벽 밑동 vs 얹힘 판정). 골든 = "밑판 기준 슬롯 자리"(로봇 자세는 매번 계산).

---

## 내일 할 것 📋 (체크리스트)

로봇이 자유로울 때(오늘은 비전용):

- [ ] **브리지 재기동 → J6 대회전 가드 활성** (`start_console.sh`)
- [ ] **골든 전량 재티칭** (마운트 교체) — 새 방식 "밑판 기준 슬롯 자리"
  - [ ] 파랑 픽·플레이스 골든
  - [ ] 노랑 픽·플레이스 골든
  - [ ] 빨강(긴벽) 픽·플레이스 골든
  - [ ] red_s 픽·플레이스 골든 (측면감시 결합)
- [ ] **형상 base_frame 검출 새 마운트 재튜닝** (`base_depth_corner.py`·`edge_place.py`, base_view 자세서 σ 재측정)
- [ ] **측면 삽입감시 튜닝** (`side_slot.py`) — 벽 내리는 프레임으로 base_top/wall_bottom ROI·부호·문턱 확정
- [ ] **edge 앵커 러너 엔드투엔드** (`place_front_first.py --anchor edge`): 베이스프레임→슬롯선→벽선 정렬
- [ ] **4벽 무개입 재현** (파랑→노랑→빨강→red_s) 3연속
- [ ] 파랑 성공 로그(뒤끝 6.0→0.33mm) 보존 — 포트폴리오 근거

---

## 안전 규칙 (사고 기반, 위반 금지) ⚠️

- **기둥 꼭대기 아래 XY 횡보정 금지** (챔퍼 없어 벽 슬립 — 사고 1건).
- **|ΔJ6|>180° 이동 금지** (±175 못 넘어 320° 회전 → controller_dead). J6=0 경유 자세 삽입.
- **관절 한계** J1·J4·J5·J6 ±175° / J3 ±160°. 넘기면 MoveIt −26으로 전 이동 거부.
- **동결 복구**: ServoJ 알람14 → `fr5_rescue.sh`(ResetAllError). controller_dead(앱 사망) → 전원급.
- **부품 랙 복귀는 사람이** (로봇 자동복귀 금지, 상태만 보고).
- **실기 가동 중 같은 DDS 도메인 테스트 금지** (도메인 99 격리).

---

## 서비스·구성

| 포트 | 서비스 |
|---|---|
| :8765 | 브리지 (FR5/ZK 제어, FastAPI) |
| :8766 | 뎁스캠 D435 (손목캠) |
| :8768 | 새카메라 (뒤끝) |
| :8770 | 벽 꽂기 플레이어 |
| :8771 | 측면 카메라 (C270) |
| :8772 | 자세별 회전 뎁스 뷰어 |
| :8000 | 정적 콘솔 |

**기동**: `nmcli con up fr5-wired` → `start_cam.sh rs` → `start_cam2.sh` → `start_console.sh` → `fr5_rescue.sh`(또는 `fr5_up.sh`).
**네트워크**: 유선 192.168.58.10(로봇, 컨트롤러 .58.2) + 무선 192.168.20.10(20.x망).

## 주요 파일

- `bridge_server.py` — FR5/ZK 브리지 (MoveIt 경유, J6 가드).
- `bf2_robot_console.html` — 하모니 콘솔 (트윈·조그·리코더·카메라).
- `tools/place_front_first.py` — 앞끝우선 place 러너.
- `tools/golden.py` · `golden_place.py` — 골든 정렬·브리지 헬퍼.
- `tools/cycle_front_first.py` — 파지→운반→삽입 사이클.
- `tools/edge_place.py` · `base_depth_corner.py` · `side_slot.py` — 형상·측면 검출.
- `tools/cam_autostart.sh` · `depth_view_rot.py` · `wall_player.py` · `side_cam.py` — 인프라.
- `dot_calib.json` — 캘리브·골든 (마운트 교체로 재티칭 예정).
- `docs/` — 재설계 문서 · 지시서 v2 · 프로젝트 정리.

---

## 9/5 진행 — 베이스 자세 측정 완성 + 랙 중앙 추종 파지 (미완: 기둥↔벽점 정렬)

### 오늘 확정된 것
| 항목 | 실측 |
|---|---|
| 베이스 자세 측정(기둥 색점 4점) | 벽 0~4개 전 조건 게이트 통과 (yaw σ 0.006~0.018°, 위치 σ 0.036~0.070mm) |
| 화면→로봇 매핑 | 직교 오차 0.0°, 30mm 이동을 30.0mm 로 판독 |
| 슬롯 목표 계산 정확도 | **가로축 0.02~0.15mm · rz 0°** (길이축은 파지 위치에 의존) |
| 랙 중앙 추종 파지 | 벽 17.7mm 이동 추종 파지 실증 |
| 안착 판정(80mm) | 밑판 뎁스 −80mm 기준으로 정정 후 정상 |

### 진범 3건 (전부 "잘 되는 것처럼 보이는데 틀린" 종류)
1. **`cam_server` 원본 버퍼 오염** — 오버레이를 RealSense 재활용 버퍼에 직접 그려, 다음 프레임 검출 입력에 지난 그림이 섞임(/raw 30회 중 7회). **검출기가 자기가 그린 점을 다시 검출** → 몇 주간의 유령점 유력 진범. `img = img.copy()` 로 수정, 오염 0/30.
2. **노출이 플리커 비안전값** — 120Hz 조명에서 프레임마다 검출 2/4↔4/4. 플리커 안전값(83·167·250·333·417)만 쓰도록 + `health_gate` 적응 노출.
3. **taught 랙 파지가 벽 길이방향 ~9mm 중앙 이탈** — 파랑 8.57mm·빨강긴 9.01mm(가로축은 0.04~0.15mm 정확). 삽입 시 벽이 편중되어 슬립.

### 새 도구 (`tools/`)
- `pillar_dots.py` — 기둥 색점 4점(파랑2·노랑1·빨강1). 위치(꼭짓점 바깥 대각) → 꼭짓점별 색 → 200×130 모양 3단 판별. 벽 점 배제·3점 폴백·그늘 파랑 2차 밴드.
- `base_pose_check.py` — 베이스 자세 게이트(조건별 `--label` 비교)
- `base_twist.py` — ArUco 고정판 기준 베이스 틀어짐(분해능 0.03mm·0.007°)
- `color_lock.py` — 노출 점수화·기억·복원·감시(복원만, 자동 재탐색 금지)
- `slot_target.py` / `place_calc.py` — 기둥 px → 로봇 → 슬롯 목표 TCP, 랙 관측 캘리브 파지, 막힘 감시 하강
- `pillar_view.py`(:8773) / `rack_view.py`(:8774) — 기둥·랙 검출 실시간 뷰(콘솔 패널)

### 🔴 다음 세션 1순위 — 기둥점 ↔ 벽점 정렬 보정
랙의 "색점 두 끝 중간"은 점이 대칭이 아니면 **벽의 물리적 중앙이 아니다**(실측 ~9mm 차이). 근본 해법은 **호버에서 든 벽 점과 기둥 점을 같은 화면에서 맞추는 것** — 그리퍼가 벽 어디를 물었든 무관해진다.
- z440 에서는 기둥이 화면 밖 → **둘 다 보이는 정렬 자세를 새로 티칭**해야 함
- 절차: 정렬 자세에서 [벽점↔기둥점] 관계를 성공 상태로 1회 저장 → 매 사이클 Δ 로 XY·rz 보정 후 하강
상세: `docs/session-0905-base-pose-color-pillars.md`

### 상태 파일 (`state/`)
랙 캘리브·관측자세·매핑·앵커·노출 등 오늘 확정된 설정값. 실기 복구 시 `~/bf2_console/` 로 되돌리면 됨.


---

## 9/5 밤 (로봇 OFF) — 설계했던 보정 3개를 실제 run 경로에 배선

사용자 지적: 오늘 말한 보정(기둥점↔벽점 같은 화면 비교 / 잡은 뒤 뎁스로 벽 길이 검산 / 베이스 전체 보는 위치에서 정렬 후 z440)이 **`place_calc run` 에 하나도 없었다**. 코드로 확인 결과 사실. 삽입 성공은 계산 목표 + 사용자 육안 nudge + 막힘감시 하강의 조합이었다.

### 신설 `tools/hover_align.py` — 호버 정렬(오프라인 실증, 실기 미투입)
- z_seat+85 에서 **든 벽 점 ↔ 베이스 특징(기둥 점 + 안착된 벽 점)** 을 같은 손목캠 프레임에서 기준 관계에 맞춘다. 파지가 어디를 물었든 무관해진다.
- δ_robot = R(rz−180)·Jinv_obs·(d_h/377)·Δpx. **손목캠이라 rz 회전 매핑 필수** — 없으면 90° 틀린 답.
- 사용자 nudge 프레임 3건(노랑 z441)으로 검증: 예측 (+2.12,−0.60)/(−0.10,+0.89)/(+2.21,−1.49) vs 실제 (+2,−1)/(0,+1)/(+2,−2).
- 게이트: 특징 ≥2·벽점 ≥1·축척 3%·rms 6px·수렴 0.3mm/0.15°·**발산 즉시 정지**. `ref/check/align/test` 모드.
- red_s 는 z440(rz 83)에서 베이스가 화면에 없음 → 정렬 자세 별도 티칭 필요.

### `place_calc.py` 배선
- run: z+85 → **hover_align.align → 정렬된 현재 TCP 로 하강**. 기준(`hover_ref_0905.json`) 없으면 하강 금지(`--no-align` 만 우회).
- 파지 축척: 든 벽 뎁스 d_w/fx 로 가상값 0.12 대체(`held_depth <색>`). 든 벽 전체는 프레임에 안 들어와 길이는 못 잼.
- 랙 관측자세에서 뎁스 물리 길이 검산 `rack_len_check`(색점과 독립, 합성 검증 198→197.7mm). 기본 보고, `--len-gate` 면 정지. ⚠ 뎁스 계열 실기 미검증.

### 내일 첫 행동
데몬 재기동 → `run blue`(호버 정지) → z+85 사용자 육안 정렬 → `hover_align.py ref blue` → `check` Δ≈0 → 1mm/1° 조그로 부호 검증 → `run blue --seat`. 상세 `docs/session-0905-night-hover-align.md`.

### (23:3x) 설계 4단계 재검증 → `run` 고정 순서로 재작성 (모의 브리지 6/6)
① 빈 손 베이스 재확인(매 사이클, 직전 삽입이 베이스를 밀 수 있음) → ② 랙 재관측·중앙 파지(매 픽, 랙이 밀림)·rz180·파지 판정 → ③ +20mm 미끄러짐 → 파지 편차 게이트(1.0mm/0.3°) → ①베이스+파지로 목표 → 운반·재확인 → z478 → ④ z+85 두 카메라 호버 정렬 → 정렬된 TCP 로 막힘감시 하강 → 성공 시 기준 자동 승격.
설계와 다르게 한 점: "픽 후 길이"는 손목캠으론 불가(벽이 프레임보다 큼) → 랙 길이 게이트 + 파지 편차로 분리 / 베이스는 빈 손 픽 전 측정 / `WALL_DELTA`·`RZ_BIAS` 상수 제거. 검증 스크립트 `tools/tests/mock_run_place_calc.py`.
