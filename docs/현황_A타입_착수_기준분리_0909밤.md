# 9/9 밤 — A타입 착수: 기준 분리 + 파랑·레드·옐로 z440/슬롯 티칭

B타입(집 B) 5벽은 9/9 저녁에 끝났고(리프트 포함), 같은 날 밤 **A타입 티칭에 들어갔다.**
A 는 랙·벽이 B 와 같고 **베이스 스티커 색 구성도 같다**. 다른 것은 **노랑 ↔ red_s 슬롯 위치가 서로 바뀐 것**이다.

---

## 1. 기준 분리 — `state/house` 를 심볼릭 링크로

기준 파일이 색 이름(blue/yellow/red/red_s/red_in)으로만 구분돼 있어서, A 를 티칭하는 순간 B 기준을 덮어쓴다.

```
state/house      → house_a   (심볼릭 링크 = 지금 활성 타입)
state/house_a/   A타입 기준
state/house_b/   B타입 기준 (9/9 저녁 완성본 그대로)
```

- `house_cycle.py` 에 `house_type()` / `switch_house()` 복원. 화면 상단에 **집 타입 배지**, 기동 로그에 `★집타입 X` 를 띄운다. 링크는 눈에 안 보이니 항상 보이게 하는 것이 안전장치다.
- `ref_view` · `fork_carry` · `side_seat` · `hover_view` 가 `state/house` 를 하드코딩하고 있어서, **링크 방식이면 그 도구들을 하나도 안 고치고 전환된다.**
- 전환은 작업 중(busy)엔 거부. 링크가 아니면 거부.

`house_a` 는 `house_b` 를 복사한 뒤 정리했다.

| 유지 | 랙 기준 5색 · 파지 서명 · fork.json · blue/red 슬롯·호버(자리 같음) |
|:---|:---|
| 제거 | yellow·red_s·red_in 의 슬롯/호버/측면 기준(자리가 다르거나 미확인) |
| 제거 | `aruco_ref.json` — A 첫 슬롯 2/2 때 새로 만든다 |

---

## 2. 오늘 밤 고친 것 3건

### `tools/cam_autostart.sh` — 사이클 중에는 '어둡다'로 카메라를 죽이지 않는다
베이스 측정의 노출 사다리가 8~20 까지 내려가면 밝기 3~4 가 **정상**인데, 데몬이 이걸 `밝기 < 5 = 고장` 으로 보고
카메라를 재기동해 사이클을 두 번(19:14 · 19:22) 죽였다. → `:8776` 이 busy 인 동안은 밝기 판정을 건너뛴다.
얼음 판정(같은 프레임 3회)은 그대로 둔다.

### `tools/pillar_dots.py` — A타입 전용 보강 (B 경로 불변)
`house_is_a()` 가 True 일 때만 두 가지를 더 한다.
1. **청록 밴드**(H 85~112 · S 80~195 · V 25~200)를 파랑 목록에 합류 — 좌하 기둥이 한때 청록 스티커였을 때 대응.
2. **그늘 파랑을 낮은 면적 하한(15)으로 한 번 더** — 좌상 파랑이 노출 83 에서 48px 라 열림 연산 뒤 하한 40 에 깎여 사라졌다.

### `tools/pillar_view.py` — 빨강 범위가 리스트인 것을 못 받아 매 프레임 죽고 있었다
빨강은 9/6 부터 색상환 랩어라운드 때문에 `[(lo,hi),(lo,hi)]` 리스트인데 뷰는 `lo, hi = RANGES[c]` 로 풀고 있었다.
그 뒤로 뷰를 재기동한 적이 없어 드러나지 않다가, 오늘 재기동하자 **`PILLARS 0/4` 로 굳었다**(내부는 매 프레임 ValueError).
→ 구간별 여유 중 최대를 쓴다. 고친 뒤 4/4 정상.

---

## 3. A타입 티칭 진행 (파랑 · 레드 · 옐로)

절차는 세 벽 모두 같다. **랙에서 집어 z440 으로 올라가 → 사용자가 육안 정렬 → z440 기준 저장 → 하강 → 슬롯 1/2.**
B 에서 복사해 온 호버 기준은 세 벽 모두 정렬에 실패했다(특징 개수/배치가 A 와 다름). 게이트가 정상 작동한 것이다.

| 벽 | z440 기준(손목캠) | 안착 z | 슬롯 1/2 |
|:---|:---|:---:|:---|
| blue | 벽점 2 · 특징 2 · TCP (177.50, −417.51, 440.0) | 354.0 | ⚠ 미저장(그리퍼가 먼저 열려 상승) |
| red | 벽점 1 · 특징 2 · TCP (306.90, −415.98, 440.0) 노출 250 | **354.0** | ✅ (306.90, −415.98, 354.0) rz −178.70 |
| yellow | 벽점 1 · 특징 4 · TCP (243.75, −515.99, 436.0) 노출 500 | **351.0** | ✅ (243.75, −515.99, 351.0) rz −88.90 |

- **옐로는 red_s 자리로 넣었다**(사용자 지시). 슬롯 목표는 `B red_s 의 XY·rz + 옐로 벽 높이` 로 심고, 안착 z 는 사용자가 준 **351**.
- 옐로 정렬 검증: wrist Δ(+0.15, −0.12)mm 특징 5 rms 0.9px / newcam Δ(+0.01, −0.07)mm 특징 4 rms 0.2px — **두 카메라가 0.15mm 안에서 일치**.
- 세 벽 모두 **하강 중 막힘 0**.

### 기준 특징 개수가 프레임마다 흔들린다
옐로 z440 에서 6프레임을 재보니 **특징이 4~6개로 흔들렸다**(작은 점 면적 63~144 가 들락거림).
기준을 6개로 찍으면 4개만 잡히는 프레임에서 매번 멈춘다 — 오늘 레드가 `기둥 특징 1개 < 기준 4개` 로 막힌 것과 같은 구조.
→ **6/6 로 등장한 것만 남긴다**(9/8 "기준은 가장 적게 잡힌 판으로" 와 같은 원칙).

---

## 4. 오늘 밤 새로 확인한 함정

- **그리퍼가 멀리 움직이면 제어 루프가 5초 멈추고, 동결 워치독(4초)이 그걸 컨트롤러 사망으로 오판해 다음 이동을 409 로 거부한다.**
  낮에 안 걸린 이유는 직전 사이클이 그리퍼를 30 에 열어둔 채 끝나 "열기 → 30" 이 무동작이었기 때문. 재시작 뒤엔 8 이나 100 에서 출발해 9초가 걸린다.
  ros2_control 로그 실측: 그리퍼 동작마다 **2.05~2.06초**, 먼 거리면 **3.4~5.1초** 루프 정지. 아직 안 고쳤다 → §5.
- **1mm 이동은 실행되지 않는다.** "started" 응답만 오고 위치는 그대로. 5~6mm 왕복(위로 갔다 목표로)으로 넣으면 0.02mm 로 들어간다.
- `SEAT_UNKNOWN_AUTO` 는 A 티칭 동안 **False** 로 둔다. True 면 안착 판정 불가일 때 그리퍼를 열고 올라가 버려서, 그 자세에서 슬롯 1/2 을 찍을 수 없다. (파랑 1/2 을 놓친 원인)
- 레드·파랑 자리에선 **손목캠에 기둥이 1~2개만 들어온다**(벽 몸통이 가림). 옐로 자리가 4개로 제일 낫다.

---

## 5. 남은 일

1. **파랑 슬롯 1/2** — 로그의 안착 TCP (177.50, −417.51, 354.0) 로 채우거나 다시 티칭.
2. **슬롯 2/2 (레드·옐로·파랑)** — 빈손으로 관측자세에 가서 베이스와 짝을 맞춰야 확정된다. A용 `aruco_ref` 도 이때 생성.
3. **red_s · red_in** — 아직 손도 안 댐. red_s 는 옐로 자리로 간다.
4. **그리퍼 동결 오판** — 브리지 워치독에 "그리퍼 명령 진행 중(+2초)은 동결 판정 보류" 필요.
5. `SEAT_UNKNOWN_AUTO` 를 True 로 되돌리는 시점 = A 안착 기준이 다 잡힌 뒤.
6. 옐로 벽 방향(rz −88.9)이 맞는지 확인 — B 옐로는 +91.2 였고 지금은 red_s 슬롯 방향을 그대로 쓴다.

---

## 6. (23:2x) 풀사이클 속도 개선 3건 — 사용자 제안, 코드 반영·실기 미검증

오늘 19사이클 실측이 근거다.

```
BASE     n=19  중앙값 20s  최소 9s  최대 89s      ← 7/19 가 70s 초과
DESCEND  n=11  중앙값 90s  최소 84s
```

### ① 베이스 노출을 기억해 재사용 (`place_calc.health_gate`)
`health_gate` 는 4프레임 중 3프레임에서 4/4 가 나오면 바로 통과(9~20s)하고, 미달이면 `FLICKER_LADDER`
8단계를 훑는다(단계마다 1.5s + 4프레임 ≈ 22s, gain 2차 패스까지 가면 두 배). **BASE 가 9s 와 89s 로 갈리는 게 이것이다.**

매번 다시 헤매는 이유는 BASE 사이에 다른 단계가 노출을 바꿔놓기 때문이다 —
랙 관측 417 · 파지 서명 333~500 · 하강 감시 사다리 83~800. 다음 벽의 BASE 는 엉뚱한 노출에서 시작한다.

→ 4/4 로 통과한 노출·게인을 `state/house/base_expo.json` 에 저장하고(`_remember_base_expo`),
다음 BASE 진입 시 **그 값을 먼저 적용(1.5s)한 뒤** 첫 판정을 한다. 실패하면 종전 사다리로 간다.
**최악 +1.5s, 최선 −60s** 라 손해 보는 경우가 없다. 조명이 바뀌면 저장값이 안 맞아 사다리로 가는데 그건 지금과 같다.

### ② 하강 속도 — 채널 안 자유 구간만 10% (`place_calc.descend_monitored`)
전 구간 `speed(1)` 이라 3mm 스텝 하나에 4~5초, 17스텝에 90초였다.
**10mm 스텝 구간(`JAM_STEP_FAST`)만 10%** 로 올리고 진입부(처음 `JAM_FINE_HEAD`=6스텝)와
안착부(마지막 `JAM_FINE_TAIL_MM`=15mm)는 1% 그대로 둔다.

blue(z439→354) 모의: **17스텝 중 5스텝(z+67~27)이 10%**, 나머지 12스텝은 1%.

진입부를 건드리지 않는 이유 — 9/5 기둥 파손도, 9/9 막힘 4건도 전부 **z+73~76 진입부**였다.
막힘 판정 자체는 정지 상태에서 재므로 속도와 무관하고, 바뀌는 것은 접촉 순간의 관성뿐이다.

### ③ 안착 판정 불가 자동완료를 버튼으로 (`house_cycle`)
상수 `SEAT_UNKNOWN_AUTO` 를 파일(`state/house/seat_auto.json`) + 버튼으로 바꿨다. 재기동이 필요 없다.

| | 동작 | 쓸 때 |
|:---|:---|:---|
| **ON** | 목표 z 까지 막힘 없이 갔으면 [계속] 없이 개방·상승 | 본선(4벽 연속) |
| **OFF** | 그리퍼를 문 채 SEAT FAIL 로 정지 | 티칭 — 그 자세에서만 슬롯 1/2 을 찍을 수 있다 |

`not_seated`(카메라가 적극적으로 잘못 앉았다고 판정)는 어느 쪽이든 정지하고, 기준 승격도 하지 않는다.
파일이 없으면 ON(본선)이 기본. 콘솔 상단에 현재 상태를 배지로 띄운다.

**지금은 OFF** — A 티칭이 남아 있다(파랑 1/2 · red_s · red_in).

### 예상 효과
①②만으로 벽당 300초 → 200초대 초반. **실기 검증은 아직이다**(조명이 꺼져 오늘 밤엔 못 함).
내일 밝을 때 파랑 한 장으로 ①의 "기억한 베이스 노출 적용" 로그와 ②의 "하강 속도 10%" 로그를 확인할 것.
